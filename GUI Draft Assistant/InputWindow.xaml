<Window x:Class="GUI_Draft_Assistant.InputWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GUI_Draft_Assistant"
        mc:Ignorable="d"
        Title="InputWindow" Height="450" Width="800">
    <Border Margin="10">

        <StackPanel>

            <!-- TITLE -->
            <TextBlock FontSize="40" TextWrapping="WrapWithOverflow"  HorizontalAlignment="Center" TextAlignment="Center">
                Veuillez saisir les données des matchs LCS le plus récents"
            </TextBlock>

            <!-- Barre de saisie -->
            <TextBox Padding="2" Height="20" Margin="10 50" AcceptsReturn="True" KeyDown="TextBox_KeyDown"/>

            <!-- Validation Button -->
            <Button Height="30" Width="200" FontSize="18" VerticalAlignment="Center" HorizontalAlignment="Center">
                Valider
            </Button>
            
        </StackPanel>

    </Border>

    
</Window>
